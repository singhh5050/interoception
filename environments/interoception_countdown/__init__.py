# The env is installed via pyproject py-modules; both interoception_countdown and _solver
# are top-level modules, so this __init__ is only here so the directory works as a checkout
# (e.g. for `prime env install ./environments/interoception_countdown`).

